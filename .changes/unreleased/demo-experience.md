---
bump: minor
type: Added
---

A guided public demo: a walkthrough of how the tool works, statement tabs that read as navigation, matching header and footer, and optional auto-seeding of a demo wallet so the statements are never empty.
